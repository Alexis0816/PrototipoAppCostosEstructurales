# Cualquier build de producción (npm run build / npm run deploy) fuerza los datos de
# demo, sin importar si existen datos locales reales en src/data/local/. Así, el deploy
# público a GitHub Pages nunca puede filtrar datos confidenciales por accidente.
VITE_FORZAR_DATOS_DEMO=true
